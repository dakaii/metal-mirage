apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
spec:
  interval: 10m
  path: ./gitops/infrastructure
  prune: true
  sourceRef:
    kind: GitRepository
    name: metal-mirage
  timeout: 5m
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-standby
  namespace: flux-system
spec:
  interval: 5m
  path: ./gitops/apps
  prune: true
  sourceRef:
    kind: GitRepository
    name: metal-mirage
  dependsOn:
    - name: infrastructure
  # Cold standby: keep manifests, scale demo to zero until failover drill.
  # AKS: LoadBalancer so TM can target an IPv4 demo ingress (cannot mix IP + DomainName;
  # aksFqdn is the API host and is not :80/healthz).
  patches:
    - patch: |-
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: demo
          namespace: demo
        spec:
          replicas: 0
      target:
        kind: Deployment
        name: demo
        namespace: demo
    - patch: |-
        apiVersion: v1
        kind: Service
        metadata:
          name: demo
          namespace: demo
        spec:
          type: LoadBalancer
      target:
        kind: Service
        name: demo
        namespace: demo
  timeout: 5m

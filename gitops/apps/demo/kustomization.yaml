apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: demo
resources:
  - namespace.yaml
  - nginx-configmap.yaml
  - deployment.yaml
  - service.yaml
  # No Ingress: portfolio path is Azure LB → Service NodePort 30080 (no Traefik).

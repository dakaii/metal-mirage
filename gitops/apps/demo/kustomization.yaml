apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: demo
resources:
  - namespace.yaml
  - nginx-configmap.yaml
  - deployment.yaml
  - service.yaml
  # No Ingress: HTTP via NodePort 30080 (metal node IP / Azure LB / optional MetalLB).

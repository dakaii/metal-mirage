# Example only — do not commit real stack YAML.
# Prefer editing config/clusters.yaml then:
#   ./scripts/sync-baremetal-config.sh
#   ./scripts/up.sh primary
# See docs/METAL-PRIMARY.md, docs/CONFIG.md, docs/PORTABLE-ARCHITECTURE.md
name: metal-mirage-bare-metal
runtime: go
description: Thin Talos primary provisioner for real bare-metal nodes (portable L1)
#
# Inventory SoT is config/clusters.yaml (primary.nodes, dry_run, install_disk, …).
# Manual pulumi config set baremetal:* is only for debugging the sync path.
